# Train Configuration
# The Bluetooth address of your LionChief train (leave empty for mock mode)
TRAIN_ADDRESS=

# Queue timeout in seconds (default: 60 = 1 minute)
# Users will be rotated after this time, even if they are the only one in queue
TRAIN_QUEUE_TIMEOUT=60

# API URL (used by UI to connect to API)
API_URL=http://localhost:8000

# Base path for UI when hosted under a subdirectory (leave empty for root)
# Example: /lionchief for hosting at http://example.com/lionchief/
BASE_PATH=

# Admin password for control configuration (leave empty for no password)
ADMIN_PASSWORD=
